[project]
name = "autoresearch-ops"
version = "0.1.0"
description = "Operator tooling for Modal-backed autoresearch runs"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.10.8",
    "modal",
    "numpy>=2.2.6",
    "pandas>=2.3.3",
]
